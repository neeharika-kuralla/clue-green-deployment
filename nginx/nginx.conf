events {}

http {
    upstream backend {
        server host.docker.internal:3002;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
}